<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
  <%- include('partials/_header') %>
    <style>
    body {
      background-color: #B22B00;
    }


    .button {
      display: inline-block;
      border-radius: 4px;
      background-color: #00FFF2;
      border: none;
      color: #000000;
      text-align: center;
      font-size: 16px;
      padding: 20px;
      width: 100px;
      transition: all 0.5s;
      cursor: pointer;
      margin: 5px;
    }

    .button span {
      cursor: pointer;
      display: inline-block;
      position: relative;
      transition: 0.5s;
    }

    .button span:after {
      content: '\00bb';
      position: absolute;
      opacity: 0;
      top: 0;
      right: -20px;
      transition: 0.5s;
    }

    .button:hover span {
      padding-right: 25px;
    }

    .button:hover span:after {
      opacity: 1;
      right: 0;
    }

    input[type=url]:focus {
      background-color: lightblue;
    }

    input[type=button],
    input[type=submit],
    input[type=reset] {
      background-color: #00FFF2;
      border: none;
      color: black;
      padding: 16px 32px;
      text-decoration: none;
      margin: 4px 2px;
      cursor: pointer;
    }
    </style>
</head>

<body>
  <main>
    <br>
    <br>
    <% if(user_id === urls[shortURL].poster){%>
      <h1><%= `ShortURL: ${shortURL}`%></h1>
      <br>
      <form action=<%='/urls/' + shortURL %> method="POST" style=display:inline>
        <label for="longURL">
          <font size="5"><b>Enter a updated URL:</label>
        <input id="longURL" type="url" name="longURL" placeholder="http://" style="width: 300px">
        <input type="submit" value="Update">
      </form>
      <br>
      <br>
      <form method="POST" action=<%='/urls/' + shortURL + '/delete'%>>
        <button class="button " style="text-align:center;"><span>Delete URL</span></button>
      </form>
      <%}else if(user_id === undefined  || user_id === null){%>
        <h1><%= `ShortURL: ${shortURL}`%></h1>
        <h2>This ShortLink was added by a user, and is unable to be edited! Please login or register!</h2>
        <%}else{%>
          <h1><%= `ShortURL: ${shortURL}`%></h1>
          <h2>This ShortLink was added by another user, and is unable to be edited!</h2>
          <%};%>
  </main>
  <br>
  <br>
  <a href="/urls">
    <font size="7">Home</a>
</body>

</html>